services:
  discipline:
    build: .
    container_name: discipline
    restart: unless-stopped
    ports:
      - "${DISCIPLINE_PORT:-8899}:8899"
    environment:
      - TZ=Asia/Shanghai
      - EARLY_RISE_TOKEN=***
      - EARLY_RISE_DB=/app/data/earlyrise.db
      - EARLY_RISE_STATIC=/portal
      - OBSIDIAN_VAULT=/vault
      - HERMES_HOME=/hermes-home
      - DISCIPLINE_WEBHOOK_KEY=${DISCIPLINE_WEBHOOK_KEY}
    volumes:
      - ./data:/app/data
      - ${OBSIDIAN_VAULT_PATH:-~/Documents/Obsidian Vault}:/vault:ro
      - ${HERMES_DATA_PATH:-/opt/hermes-data}:/hermes-home:ro
